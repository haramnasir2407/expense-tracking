bg tasks have system constraints like:

- network
- battery

why use tamagui?

work the same on both React Native and React web.
100% of Tamagui features work at both runtime and compile-time, which makes it both easy to use and fast.
it doesn’t need any special bundler configuration to work on either native or web
Built-in Theming System
Offers excellent TypeScript support and a familiar, token-based styling approach that speeds up development. 
