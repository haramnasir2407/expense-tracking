/**
 * Default monthly budget amounts per category when no real budget is set.
 * Used for analytics "Spending vs budget" when a category is selected.
 */
export const DUMMY_MONTHLY_BUDGETS: Record<string, number> = {
  "Food & Dining": 400,
  Transportation: 200,
  Shopping: 300,
  Entertainment: 150,
  "Bills & Utilities": 350,
  Healthcare: 100,
  Travel: 200,
  Other: 100,
};
